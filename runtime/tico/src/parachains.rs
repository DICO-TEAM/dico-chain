use super::*;

pub mod listen {
		pub const PARA_ID: u32 = 2022;
		pub mod like {
			pub const ASSET_ID: u32 = 13;
			pub const TOKEN_SYMBOL: &[u8] = "LIKE".as_bytes();
		}
		pub mod lt {
			pub const ASSET_ID: u32 = 12;
			pub const TOKEN_SYMBOL: &[u8] = "LT".as_bytes();
		}
	}
